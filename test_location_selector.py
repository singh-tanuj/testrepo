import pytest from selenium import webdriver from selenium.webdriver.common.by import By @pytest.mark.parametrize("user_state, expected_text", [  ("guest", "Enter your PIN code"),  ("logged_in_no_pin", "Enter your PIN code"),  ("logged_in_with_pin", "560001") # Example PIN code ]) def test_location_selector_visibility(user_state, expected_text):  """  TC001: Verify location selector is visible on homepage load  - Checks for guest, logged-in with/without saved PIN  """  # Setup  driver = webdriver.Chrome()  driver.get("https://www.amazon.in/")   # Simulate user state (pseudo-code, replace with actual login/session logic)  if user_state == "logged_in_with_pin":  # Login and set PIN code (implement as needed)  pass  elif user_state == "logged_in_no_pin":  # Login without PIN code (implement as needed)  pass  # For guest, do nothing  # Test steps  location_selector = driver.find_element(By.ID, "glow-ingress-block") # Amazon.in location selector ID  assert location_selector.is_displayed(), "Location selector should be visible in header"  assert expected_text in location_selector.text, f"Expected text '{expected_text}' in location selector"  # Teardown  driver.quit()